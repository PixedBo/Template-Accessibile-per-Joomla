<?xml version="1.0" encoding="UTF-8"?>
<project name="tpl_templateaccessibileperjoomla" default="dist" basedir=".">

    <!-- Project properties -->
    <property name="VERSION" value="1.0.16"/>
    <tstamp>
        <format property="DATE" pattern="yyyy-MM-dd"/>
    </tstamp>
    <property name="build" value="_build"/>
    <property name="dist" value="_dist"/>
    <property name="filename" value="tpl_templateaccessibileperjoomla_v${VERSION}.zip"/>

    <!-- Initialize build directories -->
    <target name="init" depends="clean">
        <mkdir dir="${build}"/>
        <mkdir dir="${dist}"/>
    </target>

    <!-- Copy template files to build dir, replacing version/date tokens -->
    <target name="prepare">
        <!-- Text files: token replacement applies -->
        <copy todir="${build}">
            <fileset dir=".">
                <include name="README.md"/>
                <include name="error.php"/>
                <include name="index.php"/>
                <include name="joomla.asset.json"/>
                <include name="offline.php"/>
                <include name="script.php"/>
                <include name="templateDetails.xml"/>
                <include name="assets/**"/>
                <include name="css/**"/>
                <include name="helpers/**"/>
                <include name="html/**"/>
                <include name="js/**"/>
                <include name="language/**"/>
                <include name="svg/**"/>
            </fileset>
            <filterchain>
                <replacetokens>
                    <token key="version" value="${VERSION}"/>
                    <token key="date" value="${DATE}"/>
                </replacetokens>
            </filterchain>
        </copy>

        <!-- Binary files: copied as-is, no filterchain (text-mode read corrupts binary) -->
        <copy todir="${build}">
            <fileset dir=".">
                <include name="template_preview.png"/>
                <include name="template_thumbnail.png"/>
                <include name="fonts/**"/>
                <include name="images/**"/>
            </fileset>
        </copy>
    </target>

    <!-- Create distribution ZIP -->
    <target name="dist" depends="init, prepare">
        <zip destfile="${dist}/${filename}">
            <fileset dir="${build}"/>
        </zip>
        <echo message="Package created: ${dist}/${filename}"/>
    </target>

    <!-- Clean build directories -->
    <target name="clean" description="Clean build directories">
        <delete dir="${build}"/>
        <delete dir="${dist}"/>
    </target>

</project>